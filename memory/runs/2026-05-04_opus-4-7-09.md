- **Goal:** Iteration #9 — X-Request-Id middleware with safe pass-through + log injection prevention.
- **Did:** New `apps/api/src/request-id.ts`: onRequest hook that mints UUID v4 or passes through inbound id sanitized via `^[A-Za-z0-9_-]{6,128}$`. Reflected on response. Module-augments `FastifyRequest.requestId`. 4 tests: generation, pass-through, log-injection rejection, uniqueness.
- **Result:** SUCCESS. Tests 125 → 129. All gates green.
- **Surprise:** Returning a sanitized id when inbound is malicious is better than 400-ing — it preserves the contract while blocking the attack vector silently.
- **Next:** #10 — security hardening.
